class Solution(object):
    def isPalindrome(self, x):
        str1=str(x)
        substring= str1[::-1]

        if substring==str1:
         return True
        else:
         return False
        
